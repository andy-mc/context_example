export const AddFaces = () => {
  const handleAddFace = () => {
    console.log('handleAddFace:', handleAddFace)
  }
  
  return (
    <button onClick={handleAddFace}>
      Add Face
    </button>
  );
}
